def shortest_path(M,start,goal):
    # Inputs: M - the given graph, which is a map.
    print("shortest path called")
    return

def adjacency(M, start, goal):
    for adjacent_list in map.roads:
        if start in adjacent_list:
            for node in adjacent_list:
                score = get_score()
        
    # compare the scores somewhere
    
def get_score(M, node):
    # use M.intersections to calculate the score.
    pass
def heuristic():
    pass

    
simple_test = []