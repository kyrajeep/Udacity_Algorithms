from node_huffman import node_huffman  
# If you import an interface / class you have 
# access to the class functions -_-
# input: information (words) to be encoded.
# output: a min heap as a priority queue!
import heapq
class minheap:
    # a function to remove nodes
    # a function to create a node
    # a function to check if there is only one node
    # a function to compare values to know where to insert or remove.
    def __init__():
        self.root = None

#this is cool. I didn't instantiate node_huffman yet.
node_huffman.dummy()
minheap = heapq.heapify([('k1',3),('k2',6)])
print(minheap)



